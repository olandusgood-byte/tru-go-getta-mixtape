name: Game V1.31 Street Life Smoke

on:
  push:
    branches:
      - feat/game-v1-31-street-life-20260918
    paths:
      - 'game/**'
      - '.github/workflows/game-v1-31-browser-smoke.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  chromium-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Playwright Chromium
        run: |
          python -m pip install --upgrade pip
          pip install playwright
          python -m playwright install --with-deps chromium

      - name: Run V1.31 Street Life browser smoke
        run: python game/tests/browser_v131_smoke.py
